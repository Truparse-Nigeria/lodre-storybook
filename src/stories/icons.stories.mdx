import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import * as Icons from "../icons";

<Meta title="Icon" />

Here's some _markdown_!

# Preview

<Preview>
  <Story name="icons">
    {Icons.map((el:SVGElement, index: number) => (
        {el}
    ))}
  </Story>
</Preview>

# Props

<Props of={Icons} />
